#include<stdio.h>
#include<string.h>
int main ()
{
	        char str1[50]="saikiran";
		        char str2[50]="iam gonna to copy";
			        strcpy(str1,str2);
				        printf("string s1 is:%s",str1);
					        return 0;
}
